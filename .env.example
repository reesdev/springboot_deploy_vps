# ====================================
# Environment Variables Template
# ====================================
# For local development: cp .env.example .env
# NEVER commit .env to git

# Required
DOCKERHUB_USERNAME=your-dockerhub-username

# Port configuration
# For this exercise, APP_PORT is a single source of truth:
# - Spring listens on APP_PORT inside the container
# - Docker publishes APP_PORT on the host
APP_PORT=9000

# Application Variables (Optional)
TEST_FULLNAME=Your Name
TEST_EMAIL=your.email@example.com

# Optional (defaults to dev)
SPRING_PROFILES_ACTIVE=dev

# ====================================
# MySQL (used by docker-compose + Spring)
# ====================================
# When running via docker-compose, DB_HOST should be the service name: mysql
DB_HOST=mysql
DB_PORT=3306
DB_NAME=db_store
DB_USER=appuser
DB_PASSWORD=apppassword
DB_ROOT_PASSWORD=rootpassword

# ====================================
# Redis (used by docker-compose + Spring)
# ====================================
# When running via docker-compose, REDIS_HOST should be the service name: redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
